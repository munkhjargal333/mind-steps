// features/entries/types/index.ts
// Entries feature-specific types.
// Domain types shared across the whole app live in @/types.

export type {
  JournalEntry,
  PaginatedEntries,
} from '@/types';
